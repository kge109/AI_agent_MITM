version: '3.8'
services:
  agent_b:
    build: ./agent_b
    container_name: agent_b-1
    networks:
      - agent_net

  agent_a:
    build: ./agent_a
    container_name: agent_a-1
    depends_on:
      - agent_b
    networks:
      - agent_net

networks:
  agent_net:
    driver: bridge